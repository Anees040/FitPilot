<USER_REQUEST>
ROLE: Senior Flutter + Node engineer on FitPilot. Fix photo food scan and label scan end-to-end. Follow steps IN ORDER. After each step run: flutter analyze, then flutter test. PowerShell shell - never use grep, &&, or bash syntax. Never print or move API keys. All colors via theme extensions (theme_usage_test enforces this).

VERIFIED FACTS (from an external code review - act on them, do not re-investigate):
F1. lib/data/remote/ai_food_service.dart reads PROXY_URL via String.fromEnvironment with default http://127.0.0.1:3000. env.json had no PROXY_URL, so on a real phone the app called the phone itself -> SocketException -> "Couldn't reach the server". The owner is adding PROXY_URL=https://fitpilot-js0j.onrender.com to env.json.
F2. No timeout on the HTTP call, and the Render free instance sleeps; the first request after idle takes 30-60 s.
F3. Photos upload FULL SIZE as base64 (ResolutionPreset.high, no compression). flutter_image_compress 2.5.1 is already in pubspec.yaml but never imported anywhere.
F4. in_app_camera_view.dart _takePicture() awaits the ENTIRE upload+AI call inside the shutter's _isProcessing state, so the live preview stays up and the user must hold the camera still watching a spinner inside the shutter button.
F5. Food Label mode is on-device ML Kit OCR (never calls the server). Its failures are framing/quality plus a strict parser.
F6. The app hardcodes the 429 error as "Daily photo limit reached (3/day)" but the server allows 20/day.

STEP 1 - Env plumbing
1a. lib/core/config/env.dart: add  static const proxyUrl = String.fromEnvironment('PROXY_URL');  and  static bool get hasProxy => proxyUrl.isNotEmpty;
1b. ai_food_service.dart: use Env.proxyUrl instead of its own String.fromEnvironment. If Env.hasProxy is false, throw immediately with the exact message: Server not configured: PROXY_URL missing from env.json. This message must surface in the error UI - not a generic network error.
1c. env.example.json: add "PROXY_URL": "https://fitpilot-js0j.onrender.com" and "WEB_CLIENT_ID": "<google web client id>" so the file documents every required key.

STEP 2 - Timeout + cold-start handling in AiFoodService
2a. Add Future<bool> warmUp() -> GET {proxy}/api/health with a 4 s timeout, returns true when reachable.
2b. estimateFood(): accept an optional  void Function(String phase)? onPhase  callback (phases: 'uploading', 'waking', 'analyzing'). Before the POST, if a quick warmUp() fails, report 'waking' and poll /api/health every 5 s for up to 75 s. The POST itself gets .timeout(const Duration(seconds: 60)).
2c. Error mapping: TimeoutException / SocketException -> "Could not reach the server. Check your internet - or the free server may still be waking up; try again in a minute."  HTTP 429 -> show the message from the server response body.  Missing PROXY_URL -> message from 1b.

STEP 3 - Compress before upload
In the AI photo path (capture_screen.dart _runAiOnFile), before base64-encoding: compress with flutter_image_compress (minWidth 1280, minHeight 1280, quality 78, jpeg). On web or on any compression failure, fall back to the original bytes. debugPrint original vs compressed byte sizes. Do NOT compress the OCR/label path - ML Kit needs detail.

STEP 4 - Fix the capture UX (freeze the frame)
4a. in_app_camera_view.dart: _takePicture() must ONLY take the picture. As soon as takePicture() returns, hand the file path to the parent and reset the shutter. No network work inside this widget.
4b. capture_screen.dart: when a photo arrives in scanFood mode, immediately show a full-screen processing overlay: the captured photo, dimmed with a scrim, a centered card with spinner, title "Analyzing your food...", a phase line driven by onPhase ("Waking the server (free hosting) - up to a minute..." while waking), and a Cancel button. The user must NOT need to keep the camera still - the photo is already taken.
4c. Success -> close overlay, continue the EXISTING result flow (name, kcal range, portion, save). Failure -> overlay switches to an error state with the mapped message and two buttons: Try again (re-sends the SAME photo without re-capturing) and Retake.
4d. While a scan is in flight, ignore extra shutter taps (guard flag).
4e. Use the same overlay pattern for foodLabel mode around OCR (fast, no phase line needed).

STEP 5 - Label parser leniency
Locate the nutrition-label parsing used by _runOcrOnFile. Make it accept: "Calories", "Energy", "kcal", "kJ" (convert kJ divided by 4.184), comma decimals (120,5), colon and table layouts. Prefer per-serving over per-100g when both appear and say which was used in the result subtitle. If still unreadable: error text "Couldn't read the label. Fill the frame with the Nutrition Facts table, avoid glare, hold steady." plus an "Enter manually" button that opens manual entry with source labelScan.

STEP 6 - Camera speed
in_app_camera_view.dart: ResolutionPreset.high -> ResolutionPreset.medium (much faster shutter on budget phones; plenty of detail for Gemini and OCR).

STEP 7 - Server polish (folder server/)
7a. Make the 429 message use the real limit variable (currently DAILY_LIMIT=20) and delete the app-side hardcoded "(3/day)" string (F6) - the app must display the server's message.
7b. Move the quota counter increment so a request only counts AFTER Gemini responds successfully - failed and cold-start attempts must not burn the daily allowance.
7c. Do not change the model (gemini-2.0-flash) or the response schema.

STEP 8 - Verify and report
8a. flutter analyze: 0 issues. flutter test: all green.
8b. Run: flutter run -d chrome --dart-define-from-file=env.json  - in scanFood mode web uses the library picker; upload a clear food photo and confirm a REAL kcal estimate renders. Note the debugPrint sizes and rough response time.
8c. REPORT: every step with files touched + evidence (snippet or output). Anything unverified must be listed as NOT VERIFIED with the reason. Never claim success without evidence.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-06T01:14:34+05:00.

The user's current state is as follows:
Active Document: d:\fitpilot\env.json (LANGUAGE_JSON)
Cursor is on line: 6
Other open documents:
- d:\fitpilot\env.json (LANGUAGE_JSON)
- d:\fitpilot\lib\core\navigation\app_router.dart (LANGUAGE_DART)
- d:\fitpilot\lib\main.dart (LANGUAGE_DART)
Running terminal commands:
- flutter run --release --dart-define-from-file=env.json (in d:\fitpilot, running for 2h20m10s)
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from Gemini 3.6 Flash (High) to Gemini 3.1 Pro (High). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>